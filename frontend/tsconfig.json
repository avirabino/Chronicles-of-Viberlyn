{
  "compilerOptions": {
    "target": "es5", // Target latest ECMAScript features supported by modern browsers
    "lib": ["dom", "dom.iterable", "esnext"], // Standard browser libraries + latest ES features
    "allowJs": true, // Allow JavaScript files to be compiled
    "skipLibCheck": true, // Skip type checking of declaration files
    "strict": true, // Enable all strict type-checking options
    "forceConsistentCasingInFileNames": true, // Disallow inconsistently-cased references to the same file.
    "noEmit": true, // Do not emit output files (Next.js handles this)
    "esModuleInterop": true, // Enables emit interoperability between CommonJS and ES Modules
    "module": "esnext", // Use latest module syntax
    "moduleResolution": "node", // Use Node.js style module resolution
    "resolveJsonModule": true, // Allow importing JSON modules
    "isolatedModules": true, // Ensure each file can be safely transpiled without relying on other imports
    "jsx": "preserve", // Preserve JSX to be handled by Next.js/Babel
    "incremental": true, // Enable incremental compilation
    "baseUrl": ".", // Base directory for non-relative module resolution
    "paths": {
      "@/*": ["src/*"] // Define the @/* alias to point to the src directory
    },
    "plugins": [
      {
        "name": "next"
      }
    ],
    "types": ["node", "cypress"] // Explicitly include node and cypress types
  },
  "include": [
    "next-env.d.ts", // Next.js environment types
    "**/*.ts", // Include all TypeScript files
    "**/*.tsx", // Include all TSX files
    ".next/types/**/*.ts", // Include types generated by Next.js build
    "cypress/**/*.ts",
    "cypress/**/*.tsx"
  ],
  "exclude": ["node_modules"] // Exclude node_modules directory
}
